/**
 * Phase MIG-S5 — SOS Inventory default field mappings.
 */

import type { FieldMapping, ParsedSnapshot } from "@/lib/migrations/core/types";

export function getSOSDefaultMappings(
  _snapshot: ParsedSnapshot,
): FieldMapping[] {
  return [];
}
